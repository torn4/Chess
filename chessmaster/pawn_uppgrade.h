#include <utility>
#include <string>
#include <vector>
#include <map>
class pawn_uppgrade{
    public:
std::map<std::string,int> pawnupp(std::map<std::string,int> memory_map,std::vector<std::string>id_memory,std::string piece_name, int move);


};
