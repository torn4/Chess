#include <map>
#include <string>
#include <vector>

class king_rule{
    public:
bool kingrule(std::string piece_name, std::map<std::string,int> memory_map,int move, std::vector<std::string> id_memory);
};
