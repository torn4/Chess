#include <map>
#include <string>
#include <vector>

class knight_rule{
    public:
bool knightrule(std::string piece_name, std::map<std::string,int> memory_map,int move);
int width;
int height;
int mwidth;
int mheight;
int number;
};
